var twoSum = function(nums, target) {
// initialize empty hash map
const hashmap = {};

// for each element (value) at index i in the array:
    // calculate complement = target - value
    // if complement exists as a KEY in the hash map:
        //return [hash map[complement], i]
    //else
        // store value as key and i as value in hash map
// return no solution found
};
